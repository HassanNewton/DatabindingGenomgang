<Window x:Class="DatabindingExample.Formulär"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DatabindingExample"
        mc:Ignorable="d"
        Title="Formulär" Height="450" Width="800">
    <Grid>
        <StackPanel>
            <TextBox x:Name="txtBoxName" HorizontalAlignment="Left"
          Text="{Binding FirstName, UpdateSourceTrigger=PropertyChanged}" 
          VerticalAlignment="Top" 
          Width="120"/>

            <TextBox x:Name="txtBoxSurName" HorizontalAlignment="Left" 
          Text="{Binding LastName, UpdateSourceTrigger=PropertyChanged}" 
          VerticalAlignment="Top" 
          Width="120"/>

            <Label x:Name="showLabel" Content="{Binding FullName}" 
                 HorizontalAlignment="Left" 
                 VerticalAlignment="Top"
                 Width="200"/>
        </StackPanel>
       
    </Grid>
</Window>
